const fs = require('fs');
const path = require('path');

const text = fs.readFileSync(path.basename(__filename));


let command = process.argv[2];
if (command === )
const arr = text.toString().split('\n');
console.log(arr.length - 1);
console.log(arr.length - 1);
